{
  "name": "search-for-itunes",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "docker-compose -f docker-compose.yml build; docker-compose -f docker-compose.yml up",
    "test": "echo \"Error: Did not have time to do\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "chai": "^4.2.0",
    "chai-http": "^4.2.0",
    "jest": "^23.6.0",
    "mocha": "^5.2.0"
  }
}
